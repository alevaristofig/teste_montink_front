import { createSlice } from "@reduxjs/toolkit";

const initialState = {}

export const produtoSlice = createSlice({
    name: 'produto',
    initialState,
    reducers: {}
});

export const {} = produtoSlice.actions;

export default produtoSlice.reducer;